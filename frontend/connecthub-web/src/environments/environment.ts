// Configuración por defecto: la que usa `ng build` (producción).
// En `ng serve` este archivo se sustituye por environment.development.ts
// (ver fileReplacements en angular.json).
export const environment = {
  production: true,
  // TODO: cambiar por el host real del API cuando exista un despliegue.
  apiHost: 'https://localhost:7088',
};
