// Configuración de desarrollo (ng serve). Si tu `dotnet run` muestra otro
// puerto HTTPS, cámbialo aquí: es el único lugar donde vive el host del API.
export const environment = {
  production: false,
  apiHost: 'https://localhost:7088',
};
